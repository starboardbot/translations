{
  "name": "@starboard/i18n",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/types.js && tsc",
    "dev": "node --watch ./scripts/types.js"
  },
  "dependencies": {
    "intl-messageformat": "^10.7.18"
  },
  "devDependencies": {
    "@formatjs/icu-messageformat-parser": "^2.11.4"
  }
}
